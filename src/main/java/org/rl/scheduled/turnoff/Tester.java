package org.rl.scheduled.turnoff;

public class Tester {
//		ObjectMapper mapper = new
//	ObjectMapper();
////Object to JSON in String
//		String jsonInString = mapper.writeValueAsString(Arrays.asList(createFaixaHorários(), createFaixaHorários()));
//
//		List list = mapper.readValue(MainController.class.getResource("/config.json"), List.class);
	public static void main(String[] args) {


	}
}